import 'package:flutter/material.dart';

/// The Nidly brand palette: a handful of punchy, warm colors on a light,
/// cream base — deliberately not dark, not corporate-blue.
class AppColors {
  AppColors._();

  static const cream = Color(0xFFFFF8EF);
  static const surface = Color(0xFFFFFFFF);
  static const ink = Color(0xFF2B2118);
  static const inkSoft = Color(0xFF6B6058);

  static const coral = Color(0xFFFF6F59); // primaire — chaleureux, énergique
  static const teal = Color(0xFF1FB6A6); // secondaire — frais
  static const sunshine = Color(0xFFFFC857); // accent — solaire
  static const plum = Color(0xFF8E7CC3); // tertiaire — doux

  static const green = Color(0xFF3FBE72);
  static const red = Color(0xFFFF5A5F);
  static const sand = Color(0xFFB8AFA3);
}
