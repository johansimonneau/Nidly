{
  "buildCommand": "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git /tmp/flutter-sdk && export PATH=\"/tmp/flutter-sdk/bin:$PATH\" && flutter config --no-analytics --no-cli-animations && flutter build web --release --no-web-resources-cdn",
  "installCommand": "echo 'skipping default install, Flutter SDK is fetched in buildCommand'",
  "outputDirectory": "build/web",
  "framework": null
}
